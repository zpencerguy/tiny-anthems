services:
  web:
    volumes:
      - ./secrets/gcp-service-account.json:/run/secrets/gcp-service-account.json:ro

  worker:
    volumes:
      - ./secrets/gcp-service-account.json:/run/secrets/gcp-service-account.json:ro
